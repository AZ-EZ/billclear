{
  "name": "billclear",
  "version": "1.0.0",
  "private": true,
  "description": "BillClear static Cloudflare Pages app for caregivers and active-treatment patients.",
  "scripts": {
    "dev": "python3 -m http.server 8788 --directory dist",
    "deploy": "npx wrangler pages deploy dist --project-name billclear --branch main",
    "icons": "node scripts/generate-pwa-icons.mjs"
  }
}
